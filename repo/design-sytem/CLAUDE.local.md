# 사용자 To-do (Changesets / Release 셋업)

> 이 파일은 gitignore. 개인 메모 / 외부 작업 체크리스트.

## 디자인 시스템 배포 작업 메모

만든 UI 부품들을 npm(개발자용 공용 창고)에 올려서 다른 프로젝트에서도 쓸 수 있게 하는 작업을 했다.
처음엔 최신 방식(열쇠 없이 GitHub 에서 바로 올리기)으로 하려고 했는데, 우리가 쓰는 도구가 이 방식을 아직 지원 안 해서 결국 예전 방식(비밀 열쇠 발급받아서 쓰기)으로 돌아갔다. 나중에 도구가 업데이트되면 다시 최신 방식으로 바꾸면 된다.

### 지금까지 한 거:

npm 에 dnd-lab 이라는 이름으로 워크스페이스 개설
첫 상품 두 개 수동으로 올려봄 (잘 됨)
GitHub 에 자동화용 토큰 하나 등록 완료

### 아직 남은 거:

npm 에서 토큰 하나 더 발급받아서 GitHub 에 등록
첫 정식 배포하고 외부 프로젝트에서 설치 테스트
설정 파일 원상복구 (배포하느라 잠깐 바꿔뒀음)
